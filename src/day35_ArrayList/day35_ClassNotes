07/27/2020
Topics: ArrayList Continue

class starts at: 7:10 pm

Package name: day35_ArrayList

Warmup tasks:
    1. write a program that can print the list of integers in reversed order
        ex:
            list=> {1,2,3,4,5}
            output: 5 4 3 2 1


    2. write a program that can return the sum of all the digits from a string
            ex:
                input: "a1b2c3"
                output: 6
                    (1+2+3= 6)

                input: "Today's date is 04/27/2020"
                output: 17
                    (0+4+2+7+2+0+2+0=17)

            HINT: on ascii table, the characters between #48 ~ #57 are digits


    3. write a program that can combine two String arrays into one arrayList
            ex:
                arr1 = {"A", "B", "C"};
                arr2 = {"D", "E", "F", "G"};

                list ==> {"A", "B", "C", "D", "E", "F", "G"}








main topics:
    1. Operators:
    2. If statement(if&else, else if): day9 & 10
    3. Switch: day12
    4. Loops (while & for loop, less nested loop): day18 & 19
    5. String: substring, indexof, equals, replace, contains, charAt
                    day15&16

    6. Array (single dimensional Array):
                day24 & 27

    7. Methods (void, return, method overloading):
            day29 & day30





3 data structures:
        1, Array
        2. Collection
        3. Maps



ArrayList methods:

    set(index, newElement): replaces the old element at given index with new element

    clear(): clears the list. sets the size to zero

    remove(int primitive): removes the element at the given index. ONLY REMOVES ONE
            index num

    remove(non-primitive): first matching element will be removed from the list, ONLY REMOVES ONE

                        NOT THE INDEX

                        returns boolean,

